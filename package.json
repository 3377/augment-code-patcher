{
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --target=node22 --outfile=dist/augment-interceptor.js --format=iife --charset=utf8"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
